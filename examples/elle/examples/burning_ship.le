use std/raylib;
use std/prelude;

const SCREEN_FACTOR = 80;
const SCREEN_WIDTH = 8 * SCREEN_FACTOR;
const SCREEN_HEIGHT = 8 * SCREEN_FACTOR;

fn main() {
    rl::init_window(SCREEN_WIDTH, SCREEN_HEIGHT, "UV Test - Fixed");
    defer rl::close_window();
    rl::set_target_fps(60);

    shader := rl::load_shader(nil,"./resources/burning_ship.fs");
    defer rl::unload_shader(shader);

    // Get the default texture to draw with proper UV coordinates
    tex := rl::get_default_texture();

    while !rl::window_should_close() {
        rl::begin_drawing();
            rl::clear_background(RED);

            rl::begin_shader_mode(shader);
                // Draw using DrawTexturePro to get proper UV coordinates (0,0) to (1,1)
                // DrawRectangle uses a small pixel from the shapes texture atlas,
                // so all corners get nearly identical UV coords (e.g., ~0.32-0.36)
                // which makes everything appear as a single color.

                // Draw a rectangle that will show proper UV gradient
                rl::draw_texture_pro(
                    tex,
                    Rectangle { x = 0, y = 0, width = 1, height = 1 },
                    Rectangle { x = 100, y = 100, width = 400, height = 400 },
                    Vector2 { x = 0, y = 0 },
                    0, WHITE
                );

                // Draw another one in different location
                rl::draw_texture_pro(
                    tex,
                    Rectangle { x = 0, y = 0, width = 1, height = 1 },
                    Rectangle { x = 0, y = 0, width = SCREEN_WIDTH / 2, height = SCREEN_HEIGHT / 2 },
                    Vector2 { x = 0, y = 0 },
                    0, WHITE
                );
            rl::end_shader_mode();

            //rl::draw_fps(4, 4);
            //rl::draw_text("UV coords as colors: Blue(0,0)->Cyan(0,1) left, Magenta(1,0)->White(1,1) right", 4, SCREEN_HEIGHT - 24, 18, WHITE);
        rl::end_drawing();
    }
}
